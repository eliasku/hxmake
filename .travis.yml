language: haxe

sudo: true

os:
  - linux
  - osx

haxe:
  - "3.2.1"
  - development

branches:
  only:
    - develop

matrix:
  allow_failures:
    - haxe: development

install:
  - haxelib dev hxmake .

script:
  - haxelib run hxmake _
  - hxmake _
  - hxmake haxe
